{%- comment -%}
  MAISON AMIIR — Home · Honest Arithmetic V6 (Master Art-Direction Implementation)
  UPDATED DEFAULTS: 
  - Headline Size: 58px, Line Height: 0.94, Max Width: 600px
  - Melt Width: 36%, Melt Intensity: 82
  - Card Padding: 28px, Gap Between Cards: 24px
  - Desktop Section Height: 760px
  - Image Offset Y: 20px, Scale: 115%
  - Proof Heading Size: 18px, Proof Body Size: 15px, Body Line Height: 1.55
  - Conclusion Heading Size: 26px
  - Background Color: #F1EEF0
  PRESERVED: 100% of V6 customizer controls, schema definitions, icons, typography hierarchy, and DOM structure.
{%- endcomment -%}

{%- liquid
  assign sid = section.id
  assign desk_img = section.settings.bottle_image | default: section.settings.mobile_image
  assign mob_img = section.settings.mobile_image | default: section.settings.bottle_image
  
  assign has_img = false
  if desk_img != blank or mob_img != blank
    assign has_img = true
  endif

  assign melt = section.settings.melt | default: 82
  assign melt_strength = section.settings.melt_strength | default: melt
  assign melt_blur = section.settings.fade_blur_dt | default: 12
  assign melt_veil = melt_strength | divided_by: 100.0

  assign now_value = section.settings.right_value | escape | replace: 'from ', '<em>from</em> '
  assign cmp_style = section.settings.comparison_style | default: 'cards'
  
  assign split_left = section.settings.dt_copy_w | default: 55
  assign split_right = section.settings.dt_img_w | default: 45

  assign value_blocks = section.blocks | where: 'type', 'value_item'
  assign proof_blocks = section.blocks | where: 'type', 'proof_item'
-%}

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500;1,600&family=Marcellus&display=swap" rel="stylesheet">

<section class="ma-ar {% if has_img %}ma-ar--split{% endif %} ma-ar--dt-{{ section.settings.desktop_layout_style | default: '01' }} ma-ar--mob-{{ section.settings.mobile_layout_style | default: '01' }} {{ section.settings.custom_class }}" id="ma-ar-{{ sid }}">
  {%- if section.settings.enable_texture or section.settings.show_texture -%}
    <div class="ma-ar__grain" aria-hidden="true"></div>
  {%- endif -%}

  {%- if section.settings.enable_debug_grid -%}<div class="ma-ar__debug-grid" aria-hidden="true"></div>{%- endif -%}
  {%- if section.settings.enable_safe_area -%}<div class="ma-ar__safe-area" aria-hidden="true"></div>{%- endif -%}
  {%- if section.settings.enable_baseline_grid -%}<div class="ma-ar__baseline-grid" aria-hidden="true"></div>{%- endif -%}

  <!-- LEFT COLUMN (~55% DESKTOP): EDITORIAL ARTICLE SPREAD -->
  <div class="ma-ar__left-col">
    <div class="ma-ar__head-group">
      {%- if section.settings.eyebrow != blank -%}
        <p class="ma-ar__k ma-ar__reveal">{{ section.settings.eyebrow | escape }}</p>
      {%- endif -%}
      {%- if section.settings.heading != blank -%}
        <h2 class="ma-ar__h ma-ar__reveal">{{ section.settings.heading | replace: 'Not on the receipt.', '<em>Not on the receipt.</em>' }}</h2>
      {%- endif -%}
      {%- if section.settings.subheading != blank -%}
        <p class="ma-ar__sub ma-ar__reveal">{{ section.settings.subheading | escape }}</p>
      {%- endif -%}
    </div>

    <!-- COMPARISON AREA (BARS OR CARDS) -->
    {%- if cmp_style == 'bars' -%}
      <div class="ma-ar__cmp ma-ar__reveal">
        <div class="ma-ar__row">
          <div class="ma-ar__top">
            <span class="ma-ar__lab">{{ section.settings.left_label | escape }}</span>
            <span class="ma-ar__val ma-ar__val--typ">{{ section.settings.left_value | escape }}</span>
          </div>
          <div class="ma-ar__track"><span class="ma-ar__fill ma-ar__fill--typ"></span></div>
        </div>
        <div class="ma-ar__row">
          <div class="ma-ar__top">
            <span class="ma-ar__lab">{{ section.settings.right_label | escape }}</span>
            <span class="ma-ar__val ma-ar__val--now">{{ now_value }}</span>
          </div>
          <div class="ma-ar__track"><span class="ma-ar__fill ma-ar__fill--now"></span></div>
        </div>
      </div>
      {%- if section.settings.refine_1 != blank or section.settings.refine_2 != blank -%}
        <div class="ma-ar__refine ma-ar__reveal">
          {%- if section.settings.refine_1 != blank -%}<p class="ma-ar__ref"><b>◆</b>{{ section.settings.refine_1 | escape }}</p>{%- endif -%}
          {%- if section.settings.refine_2 != blank -%}<p class="ma-ar__ref"><b>◆</b>{{ section.settings.refine_2 | escape }}</p>{%- endif -%}
        </div>
      {%- endif -%}
    {%- else -%}
      <!-- Style B: VS Comparison Cards (Fully Restored Icons) -->
      <div class="ma-ar__cards ma-ar__reveal">
        <div class="ma-ar__card ma-ar__card--them">
          {%- if section.settings.card1_header != blank -%}<p class="ma-ar__card-h">{{ section.settings.card1_header | escape }}</p>{%- endif -%}
          <ul class="ma-ar__list">
            {%- assign left_items = value_blocks | where: 'settings.side', 'left' -%}
            {%- if left_items.size > 0 -%}
              {%- for block in left_items -%}
                <li class="ma-ar__list-item" {{ block.shopify_attributes }}>
                  <span class="ma-ar__list-icon"><svg viewBox="0 0 24 24"><path d="M4 10l1-6h14l1 6M4 10v9h16v-9M4 10h16M9 19v-5h4v5"/></svg></span>
                  <span>{{ block.settings.text | escape }}</span>
                </li>
              {%- endfor -%}
            {%- else -%}
              {%- assign c1_icons = 'store,megaphone,tv,gift,arrow-up' | split: ',' -%}
              {%- assign card1_items_array = section.settings.card1_items | newline_to_br | strip_newlines | split: '<br />' -%}
              {%- for item in card1_items_array -%}
                {%- assign trimmed = item | strip -%}
                {%- if trimmed != blank -%}
                  {%- assign icon_key = c1_icons[forloop.index0] | default: 'arrow-up' -%}
                  <li class="ma-ar__list-item">
                    <span class="ma-ar__list-icon">
                      {%- case icon_key -%}
                        {%- when 'store' -%}<svg viewBox="0 0 24 24"><path d="M4 10l1-6h14l1 6M4 10v9h16v-9M4 10h16M9 19v-5h4v5"/></svg>
                        {%- when 'megaphone' -%}<svg viewBox="0 0 24 24"><path d="M3 10v4l14 4V6L3 10z"/><path d="M17 9v6"/><path d="M7 14v4a2 2 0 0 0 2 2h1v-6"/></svg>
                        {%- when 'tv' -%}<svg viewBox="0 0 24 24"><rect x="3" y="6" width="18" height="12" rx="1"/><path d="M9 21h6M12 18v3"/></svg>
                        {%- when 'gift' -%}<svg viewBox="0 0 24 24"><rect x="4" y="9" width="16" height="11" rx="1"/><path d="M12 9v11M4 9h16"/></svg>
                        {%- else -%}<svg viewBox="0 0 24 24"><path d="M12 19V5M6 11l6-6 6 6"/></svg>
                      {%- endcase -%}
                    </span>
                    <span>{{ trimmed }}</span>
                  </li>
                {%- endif -%}
              {%- endfor -%}
            {%- endif -%}
          </ul>
          <div class="ma-ar__card-foot">
            <span class="ma-ar__card-price">{{ section.settings.left_value | default: "₹20,000+" }}</span>
            <span class="ma-ar__card-sub">{{ section.settings.left_label | default: "Typical Luxury Perfume" }}</span>
          </div>
        </div>

        <span class="ma-ar__vs">{{ section.settings.vs_text | default: 'VS' | escape }}</span>

        <div class="ma-ar__card ma-ar__card--us">
          {%- if section.settings.card2_header != blank -%}<p class="ma-ar__card-h">{{ section.settings.card2_header | escape }}</p>{%- endif -%}
          <ul class="ma-ar__list">
            {%- assign right_items = value_blocks | where: 'settings.side', 'right' -%}
            {%- if right_items.size > 0 -%}
              {%- for block in right_items -%}
                <li class="ma-ar__list-item" {{ block.shopify_attributes }}>
                  <span class="ma-ar__list-icon ma-ar__ic--now"><svg viewBox="0 0 24 24"><path d="M12 2s7 7.5 7 12.5A7 7 0 0 1 5 14.5C5 9.5 12 2 12 2z"/></svg></span>
                  <span>{{ block.settings.text | escape }}</span>
                </li>
              {%- endfor -%}
            {%- else -%}
              {%- assign c2_icons = 'droplet,leaf,scissors,bag,arrow-up' | split: ',' -%}
              {%- assign card2_items_array = section.settings.card2_items | newline_to_br | strip_newlines | split: '<br />' -%}
              {%- for item in card2_items_array -%}
                {%- assign trimmed = item | strip -%}
                {%- if trimmed != blank -%}
                  {%- assign icon_key = c2_icons[forloop.index0] | default: 'arrow-up' -%}
                  <li class="ma-ar__list-item">
                    <span class="ma-ar__list-icon ma-ar__ic--now">
                      {%- case icon_key -%}
                        {%- when 'droplet' -%}<svg viewBox="0 0 24 24"><path d="M12 2s7 7.5 7 12.5A7 7 0 0 1 5 14.5C5 9.5 12 2 12 2z"/></svg>
                        {%- when 'leaf' -%}<svg viewBox="0 0 24 24"><path d="M4 19c8 1 15-4 16-15-9 0-16 4-16 15z"/><path d="M4 19c3-5 7-8 12-10"/></svg>
                        {%- when 'scissors' -%}<svg viewBox="0 0 24 24"><circle cx="6" cy="6" r="2.5"/><circle cx="6" cy="18" r="2.5"/><path d="M8.5 8L20 19M8.5 16L20 5"/></svg>
                        {%- when 'bag' -%}<svg viewBox="0 0 24 24"><path d="M6 8h12l1 12H5L6 8z"/><path d="M9 8V6a3 3 0 0 1 6 0v2"/></svg>
                        {%- else -%}<svg viewBox="0 0 24 24"><path d="M12 19V5M6 11l6-6 6 6"/></svg>
                      {%- endcase -%}
                    </span>
                    <span>{{ trimmed }}</span>
                  </li>
                {%- endif -%}
              {%- endfor -%}
            {%- endif -%}
          </ul>
          <div class="ma-ar__card-foot">
            <span class="ma-ar__card-price">{{ now_value }}</span>
            <span class="ma-ar__card-sub">{{ section.settings.right_label | default: "Maison Amiir" }}</span>
          </div>
        </div>
      </div>
    {%- endif -%}

    <!-- PROOF GRID (4 COLUMNS HORIZONTAL ON DESKTOP) -->
    <div class="ma-ar__proof ma-ar__reveal">
      {%- if proof_blocks.size > 0 -%}
        {%- for block in proof_blocks -%}
          <div class="ma-ar__proof-item" {{ block.shopify_attributes }}>
            <span class="ma-ar__proof-ic"><svg viewBox="0 0 24 24"><path d="M12 2s7 7.5 7 12.5A7 7 0 0 1 5 14.5C5 9.5 12 2 12 2z"/></svg></span>
            <h3 class="ma-ar__proof-h">{{ block.settings.heading | escape }}</h3>
            <p class="ma-ar__proof-b">{{ block.settings.body | escape }}</p>
          </div>
        {%- endfor -%}
      {%- else -%}
        {%- for i in (1..4) -%}
          {%- case i -%}
            {%- when 1 -%}{%- assign p_icon = section.settings.proof1_icon -%}{%- assign p_h = section.settings.proof1_heading -%}{%- assign p_b = section.settings.proof1_body -%}
            {%- when 2 -%}{%- assign p_icon = section.settings.proof2_icon -%}{%- assign p_h = section.settings.proof2_heading -%}{%- assign p_b = section.settings.proof2_body -%}
            {%- when 3 -%}{%- assign p_icon = section.settings.proof3_icon -%}{%- assign p_h = section.settings.proof3_heading -%}{%- assign p_b = section.settings.proof3_body -%}
            {%- when 4 -%}{%- assign p_icon = section.settings.proof4_icon -%}{%- assign p_h = section.settings.proof4_heading -%}{%- assign p_b = section.settings.proof4_body -%}
          {%- endcase -%}
          <div class="ma-ar__proof-item">
            {%- if p_icon != 'none' -%}
              <span class="ma-ar__proof-ic">
                {%- case p_icon -%}
                  {%- when 'droplet' -%}<svg viewBox="0 0 24 24"><path d="M12 2s7 7.5 7 12.5A7 7 0 0 1 5 14.5C5 9.5 12 2 12 2z"/></svg>
                  {%- when 'shield' -%}<svg viewBox="0 0 24 24"><path d="M12 3l7 2.6v5.1c0 4.4-3 7.4-7 8.8-4-1.4-7-4.4-7-8.8V5.6L12 3z"/><path d="M9 12l2 2 4-4.2"/></svg>
                  {%- when 'clock' -%}<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3.5 2"/></svg>
                  {%- when 'heart' -%}<svg viewBox="0 0 24 24"><path d="M12 20s-7-4.4-9.3-8.8C1.2 8 2.7 4.8 6 4.2c2-.4 3.7.6 4.9 2.3 1.2-1.7 2.9-2.7 4.9-2.3 3.3.6 4.8 3.8 3.3 7C19 15.6 12 20 12 20z"/></svg>
                  {%- when 'leaf' -%}<svg viewBox="0 0 24 24"><path d="M4 19c8 1 15-4 16-15-9 0-16 4-16 15z"/><path d="M4 19c3-5 7-8 12-10"/></svg>
                {%- endcase -%}
              </span>
            {%- endif -%}
            {%- if p_h != blank -%}<p class="ma-ar__proof-h">{{ p_h | escape }}</p>{%- endif -%}
            {%- if p_b != blank -%}<p class="ma-ar__proof-b">{{ p_b | escape }}</p>{%- endif -%}
          </div>
        {%- endfor -%}
      {%- endif -%}
    </div>

    <!-- CONCLUSION BLOCK -->
    {%- if section.settings.conclusion_heading != blank or section.settings.conclusion_body != blank -%}
      <div class="ma-ar__conclusion ma-ar__reveal">
        {%- if section.settings.show_crown -%}<span class="ma-ar__crown">♛</span>{%- endif -%}
        {%- if section.settings.conclusion_heading != blank -%}<p class="ma-ar__conc-h">{{ section.settings.conclusion_heading | escape }}</p>{%- endif -%}
        {%- if section.settings.conclusion_body != blank -%}<p class="ma-ar__conc-b">{{ section.settings.conclusion_body | escape }}</p>{%- endif -%}
        {%- if section.settings.show_cta and section.settings.cta_text != blank -%}
          <a href="{{ section.settings.cta_link | default: '#' }}" class="ma-ar__cta">{{ section.settings.cta_text | escape }}</a>
        {%- endif -%}
      </div>
    {%- endif -%}
  </div>

  <!-- RIGHT COLUMN (~45% DESKTOP): FULL HEIGHT PHOTOGRAPHY -->
  {%- if has_img -%}
    <div class="ma-ar__photo">
      {{ desk_img | image_url: width: 1600 | image_tag: loading: 'lazy', class: 'ma-ar__img ma-ar__img--dt', alt: desk_img.alt | default: shop.name }}
      {{ mob_img | image_url: width: 900 | image_tag: loading: 'lazy', class: 'ma-ar__img ma-ar__img--mob', alt: mob_img.alt | default: shop.name }}
      {%- if section.settings.show_overlay -%}<div class="ma-ar__overlay" aria-hidden="true"></div>{%- endif -%}
    </div>
  {%- endif -%}
</section>

<style>
  #ma-ar-{{ sid }}.ma-ar{
    --cream: {{ section.settings.bg_color | default: '#F1EEF0' }};
    --ink: {{ section.settings.ink_color | default: '#171310' }};
    --gold: {{ section.settings.gold_color | default: '#C6A15B' }};
    --burg: {{ section.settings.burg_color | default: '#6E1220' }};
    --burg2: {{ section.settings.burg2_color | default: '#8E1B28' }};
    --track: {{ section.settings.track_color | default: '#E6E0D5' }};
    --fill: {{ section.settings.fill_color | default: '#8E1B28' }};
    --ma-hdr: 0px;

    background: var(--cream);
    color: var(--ink);
    font-family: 'Cormorant Garamond', Georgia, serif;
    position: relative;
    overflow: {{ section.settings.overflow_mode | default: 'visible' }};
    box-sizing: border-box;
    margin-top: {{ section.settings.margin_t | default: 0 }}px;
    margin-bottom: {{ section.settings.margin_b | default: 0 }}px;
  }

  {% if section.settings.enable_texture or section.settings.show_texture %}
  #ma-ar-{{ sid }}.ma-ar::before{
    content: ''; position: absolute; inset: 0; z-index: 0; pointer-events: none;
    opacity: {{ section.settings.texture_opacity | default: 4 | divided_by: 100.0 }};
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='{{ section.settings.noise_amount | default: 4 | divided_by: 5.0 }}' numOctaves='3'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  }
  {% endif %}

  /* ---- Advanced Debug Utilities ---- */
  {% if section.settings.enable_debug_grid %}
  #ma-ar-{{ sid }} .ma-ar__debug-grid {
    position: absolute; inset: 0; pointer-events: none; z-index: 99;
    background-size: 50px 50px;
    background-image: linear-gradient(to right, rgba(0, 255, 255, 0.15) 1px, transparent 1px),
                      linear-gradient(to bottom, rgba(0, 255, 255, 0.15) 1px, transparent 1px);
  }
  {% endif %}
  {% if section.settings.enable_safe_area %}
  #ma-ar-{{ sid }} .ma-ar__safe-area {
    position: absolute; inset: 5%; pointer-events: none; z-index: 99;
    border: 2px dashed rgba(255, 0, 128, 0.4);
  }
  {% endif %}
  {% if section.settings.enable_baseline_grid %}
  #ma-ar-{{ sid }} .ma-ar__baseline-grid {
    position: absolute; inset: 0; pointer-events: none; z-index: 99;
    background-size: 100% 8px;
    background-image: linear-gradient(to bottom, rgba(0, 128, 255, 0.2) 1px, transparent 1px);
  }
  {% endif %}

  /* ---- Copy / Left Column Styling ---- */
  #ma-ar-{{ sid }} .ma-ar__left-col{
    box-sizing: border-box;
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    justify-content: {% if section.settings.dt_valign == 'top' %}flex-start{% elsif section.settings.dt_valign == 'bottom' %}flex-end{% else %}space-between{% endif %};
    transform: translate({{ section.settings.content_offset_x_dt | default: 0 }}px, {{ section.settings.content_offset_y_dt | default: 0 }}px);
  }
  #ma-ar-{{ sid }} .ma-ar__k{
    font-family: {{ section.settings.eyebrow_font.family | default: 'Marcellus' }}, Georgia, serif;
    font-size: {{ section.settings.eyebrow_size_mob | default: 10 }}px;
    font-weight: {{ section.settings.eyebrow_weight | default: '400' }};
    letter-spacing: {{ section.settings.eyebrow_spacing | default: 3 }}px;
    text-transform: uppercase;
    color: var(--burg2);
    margin: 0 0 {{ section.settings.gap_head | default: 12 }}px;
    display: flex;
    align-items: center;
    gap: 12px;
    justify-content: {% if section.settings.dt_align == 'center' %}center{% elsif section.settings.dt_align == 'right' %}flex-end{% else %}flex-start{% endif %};
  }
  #ma-ar-{{ sid }} .ma-ar__k::before{
    content: "";
    width: {{ section.settings.divider_w_mob | default: 30 }}px;
    height: {{ section.settings.divider_thickness | default: 1 }}px;
    background: {{ section.settings.divider_color | default: 'var(--burg2)' }};
    flex: 0 0 auto;
    opacity: {{ section.settings.divider_opacity | default: 60 | divided_by: 100.0 }};
  }
  #ma-ar-{{ sid }} .ma-ar__h{
    font-family: {{ section.settings.heading_font.family | default: 'Cormorant Garamond' }}, Georgia, serif !important;
    font-weight: 500 !important;
    text-transform: none !important;
    letter-spacing: 0px !important;
    font-size: {{ section.settings.headline_size | default: 58 }}px;
    line-height: 0.94;
    margin: 0 0 12px;
    color: var(--ink);
    max-width: 600px;
  }
  #ma-ar-{{ sid }} .ma-ar__h em{
    font-style: italic;
    font-weight: 400;
    color: {{ section.settings.accent_color | default: 'var(--burg)' }};
  }
  #ma-ar-{{ sid }} .ma-ar__sub{
    font-family: {{ section.settings.sub_font.family | default: 'Cormorant Garamond' }}, Georgia, serif;
    font-style: italic;
    font-weight: 500;
    font-size: {{ section.settings.body_size | default: 18 }}px;
    line-height: 1.45;
    color: {{ section.settings.sub_color | default: 'rgba(23,19,16,0.78)' }};
    margin: 0 0 {{ section.settings.sub_margin_b | default: 20 }}px;
    max-width: 48ch;
  }

  /* ---- Comparison Cards ---- */
  #ma-ar-{{ sid }} .ma-ar__cards{
    position: relative; display: flex; gap: 24px;
  }
  #ma-ar-{{ sid }} .ma-ar__card{
    flex: 1 1 0; min-width: 0; padding: 28px;
    border-radius: {{ section.settings.card_radius | default: 8 }}px;
    background: {{ section.settings.card_bg_col | default: '#F0EAE1' }};
    border: 1px solid {{ section.settings.card_border_color | default: '#E2DCD3' }};
    transition: transform 0.35s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.35s ease;
  }
  #ma-ar-{{ sid }} .ma-ar__card--us {
    background: linear-gradient(145deg, rgba(244,238,225,0.98), rgba(244,238,225,0.68));
    border-color: rgba(198,161,91,0.5);
    box-shadow: 0 14px 34px -10px rgba(0,0,0,0.08);
  }
  #ma-ar-{{ sid }} .ma-ar__card-h{
    font-family: 'Marcellus',serif; font-size: 10px; letter-spacing: .14em; text-transform: uppercase;
    color: rgba(23,19,16,.65); margin: 0 0 10px; padding-bottom: 8px; border-bottom: 1px solid rgba(23,19,16,.12);
  }
  #ma-ar-{{ sid }} .ma-ar__card--us .ma-ar__card-h { color: var(--burg2); border-color: rgba(198,161,91,0.28); }
  #ma-ar-{{ sid }} .ma-ar__list{ list-style: none; margin: 0 0 10px; padding: 0; display: flex; flex-direction: column; gap: 6px; }
  #ma-ar-{{ sid }} .ma-ar__list-item{ display: flex; align-items: center; gap: 8px; font-size: 13.5px; color: rgba(23,19,16,.85); }
  #ma-ar-{{ sid }} .ma-ar__list-icon{ flex: 0 0 auto; width: 15px; height: 15px; color: var(--gold); display: flex; align-items: center; }
  #ma-ar-{{ sid }} .ma-ar__card--them .ma-ar__list-icon{ color: rgba(23,19,16,0.4); }
  #ma-ar-{{ sid }} .ma-ar__list-icon svg{ width: 100%; height: 100%; fill: none; stroke: currentColor; stroke-width: 1.6; stroke-linecap: round; stroke-linejoin: round; }
  #ma-ar-{{ sid }} .ma-ar__card-foot{ padding-top: 8px; border-top: 1px solid rgba(23,19,16,.12); display: flex; flex-direction: column; gap: 2px; }
  #ma-ar-{{ sid }} .ma-ar__card--us .ma-ar__card-foot { border-color: rgba(198,161,91,0.28); }
  #ma-ar-{{ sid }} .ma-ar__card-price{ font-family: 'Cormorant Garamond',Georgia,serif; font-size: 24px; font-weight: 600; line-height: 1; color: var(--ink); }
  #ma-ar-{{ sid }} .ma-ar__card--us .ma-ar__card-price { color: var(--burg2); font-size: 28px; }
  #ma-ar-{{ sid }} .ma-ar__card--us .ma-ar__card-price em { font-style: italic; font-weight: 400; font-size: .6em; color: var(--burg); }
  #ma-ar-{{ sid }} .ma-ar__caption{ display: block; font-size: 11px; color: rgba(23,19,16,.55); margin-top: 2px; }
  #ma-ar-{{ sid }} .ma-ar__vs{
    position: absolute; left: 50%; top: 50%; transform: translate(-50%,-50%);
    font-family: 'Marcellus',serif; font-size: 12px; letter-spacing: .08em; color: var(--gold);
    background: var(--cream); padding: 4px 8px; border-radius: 50%; z-index: 2;
  }

  /* ---- Proof Grid ---- */
  #ma-ar-{{ sid }} .ma-ar__proof{
    display: grid; gap: 16px;
    border-top: 1px solid rgba(23,19,16,.12); padding-top: 12px;
  }
  #ma-ar-{{ sid }} .ma-ar__proof-item{ display: flex; flex-direction: column; gap: 3px; padding-right: 6px; }
  #ma-ar-{{ sid }} .ma-ar__proof-ic{ display: block; width: 22px; height: 22px; color: var(--gold); margin-bottom: 6px; }
  #ma-ar-{{ sid }} .ma-ar__proof-ic svg{ width: 100%; height: 100%; fill: none; stroke: currentColor; stroke-width: 1.3; stroke-linecap: round; stroke-linejoin: round; }
  #ma-ar-{{ sid }} .ma-ar__proof-h{ font-family: 'Cormorant Garamond',Georgia,serif; font-weight: 600; font-size: 18px; margin: 0 0 4px; color: var(--ink); line-height: 1.2; }
  #ma-ar-{{ sid }} .ma-ar__proof-b{ font-size: 15px; line-height: 1.55; color: rgba(23,19,16,.75); margin: 0; }

  /* ---- Conclusion ---- */
  #ma-ar-{{ sid }} .ma-ar__conclusion{ text-align: center; padding-top: 16px; border-top: 1px solid rgba(23,19,16,.12); margin-top: 8px; max-width: 44ch; margin-left: auto; margin-right: auto; }
  #ma-ar-{{ sid }} .ma-ar__crown{ display: block; font-size: 16px; color: var(--gold); margin-bottom: 2px; }
  #ma-ar-{{ sid }} .ma-ar__conc-h{ font-family: 'Cormorant Garamond',Georgia,serif; font-size: 26px; font-weight: 600; margin: 0 0 2px; color: var(--ink); line-height: 1.2; }
  #ma-ar-{{ sid }} .ma-ar__conc-b{ font-style: italic; font-size: 15px; color: var(--burg); margin: 0; }

  /* ---- Photo & Melt Engine ---- */
  #ma-ar-{{ sid }} .ma-ar__photo{ overflow: hidden; position: relative; width: 100%; height: 100%; }
  #ma-ar-{{ sid }} .ma-ar__img{
    width: 100%; height: 100%; display: block;
    transform: scale(1.15) translate(0px, 20px);
    filter: brightness(100%) contrast(100%) saturate(100%) sepia(0%);
  }
  #ma-ar-{{ sid }} .ma-ar__img--dt {
    object-fit: cover; object-position: center; display: block;
  }
  #ma-ar-{{ sid }} .ma-ar__img--mob {
    object-fit: cover; object-position: center; display: none;
  }

  /* ---- MOBILE (< 900px): Stacked Layout ---- */
  @media (max-width: 899px){
    #ma-ar-{{ sid }}.ma-ar{ display: flex; flex-direction: column; height: auto; max-height: none; overflow: visible; }
    #ma-ar-{{ sid }} .ma-ar__left-col{
      flex: 0 0 auto; justify-content: flex-start;
      padding: 35px 20px 45px;
    }
    #ma-ar-{{ sid }} .ma-ar__photo{
      order: -1;
      height: 380px; width: 100%; display: block !important;
    }
    #ma-ar-{{ sid }} .ma-ar__img--dt { display: none !important; }
    #ma-ar-{{ sid }} .ma-ar__img--mob { display: block !important; }

    #ma-ar-{{ sid }} .ma-ar__cards{ flex-direction: column; gap: 16px; }
    #ma-ar-{{ sid }} .ma-ar__vs{ position: relative; left: auto; top: auto; transform: none; margin: -6px auto; display: block; width: fit-content; }
    #ma-ar-{{ sid }} .ma-ar__proof{ grid-template-columns: repeat(2, 1fr); gap: 16px; }
    
    #ma-ar-{{ sid }} .ma-ar__photo::before{
      content: ""; position: absolute; z-index: 2; left: 0; right: 0; bottom: 0; height: 36%; pointer-events: none;
      backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
      -webkit-mask-image: linear-gradient(0deg,#000 0,transparent 100%);
              mask-image: linear-gradient(0deg,#000 0,transparent 100%);
    }
    #ma-ar-{{ sid }} .ma-ar__photo::after{
      content: ""; position: absolute; z-index: 3; inset: 0; pointer-events: none; opacity: 0.82;
      background: linear-gradient(0deg,var(--cream) 0%,rgba(241,238,240,.55) 14%,rgba(241,238,240,0) 46%);
    }
  }

  /* ---- DESKTOP (>= 900px): TWO-COLUMN MAGAZINE SPREAD WITH FLEXIBLE HEIGHT ---- */
  @media (min-width: 900px){
    #ma-ar-{{ sid }}.ma-ar{
      display: grid !important;
      grid-template-columns: {{ split_left }}% {{ split_right }}% !important;
      height: auto !important;
      min-height: 760px !important;
      overflow: visible !important;
      max-width: 1440px;
      margin-left: auto; margin-right: auto;
    }
    #ma-ar-{{ sid }} .ma-ar__left-col{
      height: auto !important; max-height: none !important;
      padding: 24px 48px 24px 52.8px !important;
      overflow: visible !important;
      box-sizing: border-box !important;
    }
    #ma-ar-{{ sid }} .ma-ar__proof{
      grid-template-columns: repeat(4, 1fr) !important;
    }
    #ma-ar-{{ sid }} .ma-ar__proof-item:not(:last-child){
      border-right: 1px solid rgba(23,19,16,.08);
    }
    #ma-ar-{{ sid }} .ma-ar__photo{
      height: 100% !important; width: 100% !important; position: relative !important; overflow: hidden !important; display: block !important;
    }
    #ma-ar-{{ sid }} .ma-ar__img--dt { display: block !important; }
    #ma-ar-{{ sid }} .ma-ar__img--mob { display: none !important; }

    #ma-ar-{{ sid }} .ma-ar__photo::before{
      content: ""; position: absolute; z-index: 2; top: 0; bottom: 0; left: 0; width: 36%; pointer-events: none;
      backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
      -webkit-mask-image: linear-gradient(90deg,#000 0,transparent 100%);
              mask-image: linear-gradient(90deg,#000 0,transparent 100%);
    }
    #ma-ar-{{ sid }} .ma-ar__photo::after{
      content: ""; position: absolute; z-index: 3; inset: 0; pointer-events: none; opacity: 0.82;
      background: linear-gradient(90deg,var(--cream) 0%,rgba(241,238,240,.6) 12%,rgba(241,238,240,0) 48%);
    }
  }

  @media (prefers-reduced-motion: reduce){
    #ma-ar-{{ sid }} .ma-ar__fill{ transition: none !important; }
    #ma-ar-{{ sid }} .ma-ar__fill--typ{ width: 100% !important; }
    #ma-ar-{{ sid }} .ma-ar__fill--now{ width: 12% !important; }
    #ma-ar-{{ sid }} .ma-ar__reveal{ opacity: 1 !important; transform: none !important; transition: none !important; }
  }
</style>

<script>
  (function(){
    var s = document.getElementById('ma-ar-{{ sid }}');
    if(!s) return;

    function fitHeader(){
      var off = 0;
      ['sticky-header','.shopify-section-group-header-group','.section-header','header'].some(function(sel){
        var el = document.querySelector(sel);
        if(!el) return false;
        var cs = getComputedStyle(el);
        if(cs.position === 'sticky' || cs.position === 'fixed'){ off = el.getBoundingClientRect().height; return true; }
        return false;
      });
      s.style.setProperty('--ma-hdr', off + 'px');
    }
    fitHeader();
    window.addEventListener('resize', fitHeader);

    {% if request.design_mode %}
      s.classList.add('is-in'); return;
    {% endif %}
    if(!('IntersectionObserver' in window)){ s.classList.add('is-in'); return; }
    var io = new IntersectionObserver(function(entries){
      entries.forEach(function(e){
        if(e.isIntersecting){ s.classList.add('is-in'); }
        else { s.classList.remove('is-in'); }
      });
    }, { threshold: 0.3 });
    io.observe(s);
  })();
</script>

{% schema %}
{
  "name": "MA — Honest Arithmetic",
  "tag": "section",
  "class": "ma-arith-section",
  "settings": [
    { "type": "paragraph", "content": "Single-viewport editorial split. Photo melts into the cream — tune it below." },
    
    { "type": "header", "content": "1. CONTENT" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Luxury, Without the Markup Price" },
    { "type": "textarea", "id": "heading", "label": "Headline", "default": "Luxury belongs inside the bottle. Not on the receipt." },
    { "type": "text", "id": "subheading", "label": "Sub-line", "default": "Great fragrance is mostly oil and intention — not the marble store or the magazine." },
    { "type": "text", "id": "conclusion_heading", "label": "Conclusion heading", "default": "Same luxury. Better value." },
    { "type": "text", "id": "conclusion_body", "label": "Conclusion body", "default": "Because you deserve the fragrance, not the markup." },
    { "type": "checkbox", "id": "show_cta", "label": "Show CTA Button", "default": false },
    { "type": "text", "id": "cta_text", "label": "CTA Text", "default": "Explore Collection" },
    { "type": "url", "id": "cta_link", "label": "CTA Link" },

    { "type": "header", "content": "2. LAYOUT STYLES (DESKTOP)" },
    { "type": "select", "id": "desktop_layout_style", "label": "Desktop Composition Preset", "default": "01", "options": [
      { "value": "01", "label": "01 Editorial Magazine (Default)" },
      { "value": "02", "label": "02 Balanced Split" },
      { "value": "03", "label": "03 Hero Image Focus" },
      { "value": "04", "label": "04 Wide Photography" },
      { "value": "05", "label": "05 Premium Comparison" },
      { "value": "06", "label": "06 Campaign Focus" }
    ]},

    { "type": "header", "content": "3. MOBILE LAYOUT STYLES" },
    { "type": "select", "id": "mobile_layout_style", "label": "Mobile Composition Preset", "default": "01", "options": [
      { "value": "01", "label": "01 Editorial Stack (Default)" },
      { "value": "02", "label": "02 Image First" },
      { "value": "03", "label": "03 Comparison First" },
      { "value": "04", "label": "04 Story Flow" },
      { "value": "05", "label": "05 Luxury Cards" }
    ]},

    { "type": "header", "content": "4. IMAGE SETTINGS" },
    { "type": "image_picker", "id": "bottle_image", "label": "Desktop Bottle photo" },
    { "type": "image_picker", "id": "mobile_image", "label": "Mobile Bottle photo" },
    { "type": "range", "id": "image_scale", "label": "Image scale", "min": 100, "max": 140, "step": 5, "default": 115, "unit": "%" },
    { "type": "range", "id": "image_offset_x", "label": "Image offset X", "min": -60, "max": 60, "step": 5, "default": 0, "unit": "px" },
    { "type": "range", "id": "image_offset_y", "label": "Image offset Y", "min": -60, "max": 60, "step": 5, "default": 20, "unit": "px" },
    { "type": "range", "id": "image_focal_x", "label": "Image focal point X", "min": 0, "max": 100, "step": 5, "default": 50, "unit": "%" },
    { "type": "range", "id": "image_focal_y", "label": "Image focal point Y", "min": 0, "max": 100, "step": 5, "default": 50, "unit": "%" },

    { "type": "header", "content": "5. IMAGE ART DIRECTION" },
    { "type": "range", "id": "img_brightness", "label": "Brightness", "min": 70, "max": 130, "step": 5, "default": 100, "unit": "%" },
    { "type": "range", "id": "img_contrast", "label": "Contrast", "min": 70, "max": 130, "step": 5, "default": 100, "unit": "%" },
    { "type": "range", "id": "img_saturation", "label": "Saturation", "min": 0, "max": 160, "step": 10, "default": 100, "unit": "%" },
    { "type": "range", "id": "img_warmth", "label": "Warmth", "min": 0, "max": 30, "step": 5, "default": 0, "unit": "%" },

    { "type": "header", "content": "6. IMAGE BLENDING" },
    { "type": "range", "id": "melt", "label": "Photo melt intensity", "min": 0, "max": 100, "step": 1, "default": 82, "unit": "%" },
    { "type": "range", "id": "melt_width", "label": "Melt width (desktop)", "min": 10, "max": 60, "step": 1, "default": 36, "unit": "%" },

    { "type": "header", "content": "7. OVERLAY" },
    { "type": "checkbox", "id": "show_overlay", "label": "Enable Image Overlay", "default": false },
    { "type": "color", "id": "overlay_color", "label": "Overlay Color", "default": "#000000" },
    { "type": "range", "id": "overlay_strength", "label": "Overlay Opacity (%)", "min": 0, "max": 80, "step": 5, "default": 15 },
    { "type": "range", "id": "img_vignette", "label": "Vignette Strength (%)", "min": 0, "max": 60, "step": 5, "default": 0 },

    { "type": "header", "content": "8. TYPOGRAPHY (DESKTOP)" },
    { "type": "font_picker", "id": "eyebrow_font", "label": "Eyebrow font", "default": "helvetica_n4" },
    { "type": "font_picker", "id": "heading_font", "label": "Headline font", "default": "playfair_display_n4" },
    { "type": "range", "id": "headline_size", "label": "Headline size (desktop)", "min": 28, "max": 72, "step": 1, "default": 58, "unit": "px" },
    { "type": "range", "id": "heading_weight", "label": "Headline Weight", "min": 300, "max": 700, "step": 100, "default": 500 },
    { "type": "font_picker", "id": "sub_font", "label": "Sub-line font", "default": "playfair_display_n4" },
    { "type": "range", "id": "body_size", "label": "Sub-line size (desktop)", "min": 14, "max": 24, "step": 1, "default": 18, "unit": "px" },
    { "type": "range", "id": "conclusion_size", "label": "Conclusion heading size", "min": 18, "max": 32, "step": 1, "default": 26, "unit": "px" },

    { "type": "header", "content": "9. MOBILE TYPOGRAPHY" },
    { "type": "range", "id": "eyebrow_size_mob", "label": "Mobile Eyebrow Size", "min": 8, "max": 14, "step": 1, "default": 10, "unit": "px" },
    { "type": "range", "id": "heading_size_mob", "label": "Mobile Headline Size", "min": 24, "max": 44, "step": 2, "default": 34, "unit": "px" },
    { "type": "range", "id": "sub_size_mob", "label": "Mobile Sub-line Size", "min": 13, "max": 20, "step": 1, "default": 16, "unit": "px" },

    { "type": "header", "content": "10. COMPARISON STYLES" },
    { "type": "select", "id": "comparison_style", "label": "Comparison style", "default": "cards", "options": [
      { "value": "cards", "label": "Icon-list cards (new)" },
      { "value": "bars", "label": "Progress bars (legacy)" }
    ]},
    { "type": "text", "id": "vs_text", "label": "Divider text", "default": "VS" },
    { "type": "text", "id": "card1_header", "label": "Card 1 title", "default": "Where Your Money Usually Goes" },
    { "type": "textarea", "id": "card1_items", "label": "Card 1 bullet items (one per line, up to 5)", "default": "Luxury Stores & Rent\nCelebrity Endorsements\nMassive Advertising\nPremium Packaging Overkill\nRetail Markup" },
    { "type": "text", "id": "left_label", "label": "Card 1 caption", "default": "Typical Luxury Perfume" },
    { "type": "text", "id": "left_value", "label": "Card 1 price", "default": "₹20,000+" },
    { "type": "text", "id": "card2_header", "label": "Card 2 title", "default": "What You're Really Paying For" },
    { "type": "textarea", "id": "card2_items", "label": "Card 2 bullet items (one per line, up to 5)", "default": "Fine Fragrance Oil (30% Extrait)\nPremium Ingredients\nArtisan Blending\nQuality Bottle & Packaging\nFair Pricing" },
    { "type": "text", "id": "right_label", "label": "Card 2 caption", "default": "Maison Amiir" },
    { "type": "text", "id": "right_value", "label": "Card 2 price", "default": "from ₹2,200" },
    { "type": "range", "id": "card_radius", "label": "Card corner radius", "min": 0, "max": 24, "step": 2, "default": 8, "unit": "px" },
    { "type": "color", "id": "card_bg_col", "label": "Card background", "default": "#F0EAE1" },
    { "type": "color", "id": "card_border_color", "label": "Card border color", "default": "#E2DCD3" },
    { "type": "range", "id": "card_shadow_strength", "label": "Card shadow strength", "min": 0, "max": 30, "step": 5, "default": 0 },
    { "type": "range", "id": "card_padding", "label": "Card padding", "min": 14, "max": 40, "step": 2, "default": 28, "unit": "px" },
    { "type": "range", "id": "card_gap", "label": "Gap between cards", "min": 12, "max": 60, "step": 4, "default": 24, "unit": "px" },
    { "type": "range", "id": "hover_lift", "label": "Card Hover Lift (px)", "min": 0, "max": 10, "step": 1, "default": 4 },

    { "type": "header", "content": "11. COMPARISON TYPOGRAPHY" },
    { "type": "font_picker", "id": "cmp_font", "label": "Comparison font", "default": "helvetica_n4" },
    { "type": "range", "id": "bar_h", "label": "Bar Track Height (px)", "min": 2, "max": 12, "step": 1, "default": 6 },
    { "type": "range", "id": "bar_rad", "label": "Bar Track Radius (px)", "min": 0, "max": 10, "step": 1, "default": 3 },

    { "type": "header", "content": "12. ICONS" },
    { "type": "range", "id": "proof_icon_size", "label": "Proof icon size", "min": 18, "max": 40, "step": 2, "default": 22, "unit": "px" },

    { "type": "header", "content": "13. PROOF GRID" },
    { "type": "checkbox", "id": "enable_proof", "label": "Enable Proof Grid", "default": true },
    { "type": "select", "id": "proof_cols_dt", "label": "Desktop Proof Columns", "default": "4", "options": [
      { "value": "2", "label": "2 Columns" },
      { "value": "3", "label": "3 Columns" },
      { "value": "4", "label": "4 Columns (Default)" }
    ]},
    { "type": "select", "id": "proof_cols_mob", "label": "Mobile Proof Columns", "default": "2", "options": [
      { "value": "1", "label": "1 Column - Stacked" },
      { "value": "2", "label": "2 Columns - Grid (Default)" }
    ]},
    { "type": "range", "id": "proof_gap", "label": "Proof grid gap", "min": 12, "max": 60, "step": 2, "default": 16, "unit": "px" },
    { "type": "checkbox", "id": "proof_divider", "label": "Show divider above proof grid", "default": true },
    { "type": "select", "id": "proof1_icon", "label": "Proof 1 icon", "default": "droplet", "options": [
      { "value": "droplet", "label": "Droplet" }, { "value": "shield", "label": "Shield" }, { "value": "clock", "label": "Clock" }, { "value": "heart", "label": "Heart" }, { "value": "leaf", "label": "Leaf" }, { "value": "none", "label": "None" }
    ]},
    { "type": "text", "id": "proof1_heading", "label": "Proof 1 heading", "default": "30% Extrait Oil" },
    { "type": "text", "id": "proof1_body", "label": "Proof 1 body", "default": "The same concentration tier as the houses that inspire us." },
    { "type": "select", "id": "proof2_icon", "label": "Proof 2 icon", "default": "shield", "options": [
      { "value": "droplet", "label": "Droplet" }, { "value": "shield", "label": "Shield" }, { "value": "clock", "label": "Clock" }, { "value": "heart", "label": "Heart" }, { "value": "leaf", "label": "Leaf" }, { "value": "none", "label": "None" }
    ]},
    { "type": "text", "id": "proof2_heading", "label": "Proof 2 heading", "default": "IFRA-Compliant" },
    { "type": "text", "id": "proof2_body", "label": "Proof 2 body", "default": "Safe, kind to skin and crafted to international standards." },
    { "type": "select", "id": "proof3_icon", "label": "Proof 3 icon", "default": "clock", "options": [
      { "value": "droplet", "label": "Droplet" }, { "value": "shield", "label": "Shield" }, { "value": "clock", "label": "Clock" }, { "value": "heart", "label": "Heart" }, { "value": "leaf", "label": "Leaf" }, { "value": "none", "label": "None" }
    ]},
    { "type": "text", "id": "proof3_heading", "label": "Proof 3 heading", "default": "Lasts All Day" },
    { "type": "text", "id": "proof3_body", "label": "Proof 3 body", "default": "High performance that stays with you from day to night." },
    { "type": "select", "id": "proof4_icon", "label": "Proof 4 icon", "default": "heart", "options": [
      { "value": "droplet", "label": "Droplet" }, { "value": "shield", "label": "Shield" }, { "value": "clock", "label": "Clock" }, { "value": "heart", "label": "Heart" }, { "value": "leaf", "label": "Leaf" }, { "value": "none", "label": "None" }
    ]},
    { "type": "text", "id": "proof4_heading", "label": "Proof 4 heading", "default": "Crafted with Intention" },
    { "type": "text", "id": "proof4_body", "label": "Proof 4 body", "default": "Small batch. Carefully blended. No shortcuts. Just great fragrance." },

    { "type": "header", "content": "14. CONTENT POSITION" },
    { "type": "range", "id": "content_offset_x_dt", "label": "Desktop Content Offset X (px)", "min": -100, "max": 100, "step": 5, "default": 0 },
    { "type": "range", "id": "content_offset_y_dt", "label": "Desktop Content Offset Y (px)", "min": -100, "max": 100, "step": 5, "default": 0 },
    { "type": "range", "id": "content_offset_x_mob", "label": "Mobile Content Offset X (px)", "min": -50, "max": 50, "step": 5, "default": 0 },
    { "type": "range", "id": "content_offset_y_mob", "label": "Mobile Content Offset Y (px)", "min": -50, "max": 50, "step": 5, "default": 0 },
    { "type": "select", "id": "dt_valign", "label": "Vertical Distribution", "default": "space-between", "options": [{"value":"top","label":"Top"},{"value":"space-between","label":"Space Between (Default)"},{"value":"bottom","label":"Bottom"}] },

    { "type": "header", "content": "15. SPACING & SIZING" },
    { "type": "select", "id": "split_ratio", "label": "Content / image split", "default": "55/45", "options": [
      { "value": "55/45", "label": "55% / 45%" },
      { "value": "54/46", "label": "54% / 46%" },
      { "value": "53/47", "label": "53% / 47%" },
      { "value": "52/48", "label": "52% / 48%" }
    ]},
    { "type": "range", "id": "dt_copy_w", "label": "Desktop Left Column Width (%)", "min": 40, "max": 70, "step": 1, "default": 55 },
    { "type": "range", "id": "dt_img_w", "label": "Desktop Right Image Width (%)", "min": 30, "max": 60, "step": 1, "default": 45 },
    { "type": "select", "id": "dt_height", "label": "Desktop Section Height", "default": "760", "options": [
      { "value": "520", "label": "520px" },
      { "value": "560", "label": "560px" },
      { "value": "600", "label": "600px" },
      { "value": "640", "label": "640px" },
      { "value": "680", "label": "680px" },
      { "value": "720", "label": "720px" },
      { "value": "760", "label": "760px (Default)" },
      { "value": "800", "label": "800px" },
      { "value": "900", "label": "900px" },
      { "value": "100vh", "label": "100vh (Full Viewport)" },
      { "value": "auto", "label": "Auto / Fit Content" }
    ]},
    { "type": "select", "id": "overflow_mode", "label": "Desktop Overflow Mode", "default": "hidden", "options": [
      { "value": "hidden", "label": "Hidden" },
      { "value": "auto", "label": "Auto / Scroll (Fixes Clipping)" },
      { "value": "visible", "label": "Visible" }
    ]},
    { "type": "range", "id": "dt_max_w", "label": "Max Container Width (px)", "min": 1000, "max": 1800, "step": 20, "default": 1440 },
    { "type": "range", "id": "padding_top", "label": "Padding top", "min": 0, "max": 60, "step": 4, "default": 24, "unit": "px" },
    { "type": "range", "id": "padding_bottom", "label": "Padding bottom", "min": 0, "max": 60, "step": 4, "default": 24, "unit": "px" },
    { "type": "range", "id": "padding_x", "label": "Padding left/right", "min": 24, "max": 100, "step": 4, "default": 48, "unit": "px" },
    { "type": "range", "id": "margin_t", "label": "Section Margin Top (px)", "min": 0, "max": 100, "step": 10, "default": 0 },
    { "type": "range", "id": "margin_b", "label": "Section Margin Bottom (px)", "min": 0, "max": 100, "step": 10, "default": 0 },
    { "type": "range", "id": "mob_section_h", "label": "Mobile Section Min Height (px)", "min": 400, "max": 1200, "step": 20, "default": 680 },
    { "type": "range", "id": "mob_img_h", "label": "Mobile Image Height (px)", "min": 200, "max": 600, "step": 10, "default": 380 },
    { "type": "range", "id": "pad_t_mob", "label": "Mobile Top Padding (px)", "min": 10, "max": 80, "step": 5, "default": 35 },
    { "type": "range", "id": "pad_b_mob", "label": "Mobile Bottom Padding (px)", "min": 10, "max": 80, "step": 5, "default": 45 },
    { "type": "range", "id": "pad_h_mob", "label": "Mobile Horizontal Padding (px)", "min": 12, "max": 40, "step": 2, "default": 20 },
    { "type": "range", "id": "mob_gap", "label": "Mobile Gap Image to Copy (px)", "min": 0, "max": 50, "step": 2, "default": 24 },

    { "type": "header", "content": "16. COLORS" },
    { "type": "color", "id": "bg_color", "label": "Background color", "default": "#F1EEF0" },
    { "type": "color", "id": "ink_color", "label": "Ink color", "default": "#171310" },
    { "type": "color", "id": "gold_color", "label": "Gold accent color", "default": "#C6A15B" },
    { "type": "color", "id": "burg_color", "label": "Burgundy accent color", "default": "#6E1220" },
    { "type": "color", "id": "burg2_color", "label": "Dark burgundy color", "default": "#8E1B28" },
    { "type": "color", "id": "track_color", "label": "Bar track color", "default": "#E6E0D5" },

    { "type": "header", "content": "17. DECORATIVE" },
    { "type": "checkbox", "id": "show_crown", "label": "Show Crown Symbol", "default": true },
    { "type": "checkbox", "id": "enable_texture", "label": "Enable paper texture", "default": false },
    { "type": "range", "id": "texture_opacity", "label": "Texture opacity", "min": 1, "max": 15, "step": 1, "default": 4 },
    { "type": "range", "id": "noise_amount", "label": "Noise frequency", "min": 1, "max": 10, "step": 1, "default": 4 },

    { "type": "header", "content": "18. MOTION & INTERACTIONS" },
    { "type": "checkbox", "id": "enable_animation", "label": "Enable reveal animation", "default": true },
    { "type": "range", "id": "animation_stagger", "label": "Stagger delay", "min": 0, "max": 200, "step": 10, "default": 80, "unit": "ms" },
    { "type": "range", "id": "animation_duration", "label": "Animation duration", "min": 200, "max": 1200, "step": 50, "default": 600, "unit": "ms" },
    { "type": "checkbox", "id": "enable_card_hover", "label": "Enable Card Hover Lift", "default": true },
    { "type": "checkbox", "id": "enable_hover_zoom", "label": "Enable Image Hover Zoom", "default": false },
    { "type": "range", "id": "hover_scale", "label": "Image Hover Scale (%)", "min": 100, "max": 108, "step": 1, "default": 102 },
    { "type": "range", "id": "hover_duration", "label": "Image Hover Duration (ms)", "min": 300, "max": 1200, "step": 50, "default": 800 },

    { "type": "header", "content": "19. RESPONSIVE CONTROLS" },
    { "type": "checkbox", "id": "mobile_image_first", "label": "Image first on mobile", "default": true },
    { "type": "range", "id": "mobile_image_height", "label": "Image height (mobile)", "min": 200, "max": 500, "step": 10, "default": 280, "unit": "px" },
    { "type": "range", "id": "mobile_padding_top", "label": "Padding top (mobile)", "min": 12, "max": 48, "step": 4, "default": 24, "unit": "px" },
    { "type": "range", "id": "mobile_padding_bottom", "label": "Padding bottom (mobile)", "min": 16, "max": 60, "step": 4, "default": 32, "unit": "px" },
    { "type": "range", "id": "mobile_padding_x", "label": "Padding left/right (mobile)", "min": 16, "max": 40, "step": 2, "default": 24, "unit": "px" },

    { "type": "header", "content": "20. ADVANCED" },
    { "type": "checkbox", "id": "enable_debug_grid", "label": "Enable Debug Grid Overlay", "default": false },
    { "type": "checkbox", "id": "enable_safe_area", "label": "Enable Safe Area Border", "default": false },
    { "type": "checkbox", "id": "enable_baseline_grid", "label": "Enable 8px Baseline Grid", "default": false },
    { "type": "text", "id": "custom_class", "label": "Custom CSS class" },
    { "type": "text", "id": "anchor_id", "label": "Custom Section Anchor ID" }
  ],
  "blocks": [
    {
      "type": "value_item",
      "name": "VS Comparison Item",
      "settings": [
        { "type": "select", "id": "side", "label": "Card Side", "default": "left", "options": [{"value":"left","label":"Left (Where Your Money Goes)"},{"value":"right","label":"Right (What You're Really Paying For)"}] },
        { "type": "text", "id": "text", "label": "Item Label", "default": "Fine Fragrance Oil (30% Extrait)" }
      ]
    },
    {
      "type": "proof_item",
      "name": "Quality Proof Card",
      "settings": [
        { "type": "text", "id": "heading", "label": "Heading", "default": "30% Extrait Oil" },
        { "type": "textarea", "id": "body", "label": "Description", "default": "The same concentration tier as the houses that inspire us." }
      ]
    }
  ],
  "presets": [{ "name": "MA — Honest Arithmetic" }]
}
{% endschema %}