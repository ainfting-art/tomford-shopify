{%- comment -%}
  MAISON AMIIR — Honest Arithmetic Section V4 (Magazine Spread Architecture)
  Preserves 100% of legacy setting IDs, animations, and Theme Customizer compatibility.
  DOM ARCHITECTURE: Single-viewport 100vh desktop grid split (Left Article Spread / Right Full-Height Image).
  100% syntax validated: hex color defaults, double quotes around strings with apostrophes.
{%- endcomment -%}

{%- liquid
  assign sid = section.id
  assign has_img = false
  if section.settings.bottle_image != blank or section.settings.mobile_image != blank
    assign has_img = true
  endif

  assign melt = section.settings.melt | default: 100
  assign melt_strength = section.settings.melt_strength | default: melt
  assign melt_blur = section.settings.melt_blur | default: 12
  assign melt_veil = melt_strength | divided_by: 100.0

  assign now_value = section.settings.right_value | escape | replace: 'from ', '<em>from</em> '
  assign cmp_style = section.settings.cmp_style | default: 'cards'

  assign value_blocks = section.blocks | where: 'type', 'value_item'
  assign proof_blocks = section.blocks | where: 'type', 'proof_item'
-%}

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500;1,600&family=Marcellus&display=swap" rel="stylesheet">

<section class="ma-ar {% if has_img %}ma-ar--split{% endif %} {{ section.settings.custom_class }}" id="ma-ar-{{ sid }}">
  {%- if section.settings.enable_texture or section.settings.show_texture -%}
    <div class="ma-ar__grain" aria-hidden="true"></div>
  {%- endif -%}

  <!-- LEFT COLUMN (~55% DESKTOP): EDITORIAL ARTICLE SPREAD -->
  <div class="ma-ar__left-col">
    <div class="ma-ar__head-group">
      {%- if section.settings.eyebrow != blank -%}
        <p class="ma-ar__k ma-ar__reveal">{{ section.settings.eyebrow | escape }}</p>
      {%- endif -%}
      {%- if section.settings.heading != blank -%}
        <h2 class="ma-ar__h ma-ar__reveal">{{ section.settings.heading | replace: 'Not on the receipt.', '<em>Not on the receipt.</em>' }}</h2>
      {%- endif -%}
      {%- if section.settings.subheading != blank -%}
        <p class="ma-ar__sub ma-ar__reveal">{{ section.settings.subheading | escape }}</p>
      {%- endif -%}
    </div>

    <!-- COMPARISON AREA (BARS OR CARDS) -->
    {%- if cmp_style == 'bars' -%}
      <div class="ma-ar__cmp ma-ar__reveal">
        <div class="ma-ar__row">
          <div class="ma-ar__top">
            <span class="ma-ar__lab">{{ section.settings.left_label | escape }}</span>
            <span class="ma-ar__val ma-ar__val--typ">{{ section.settings.left_value | escape }}</span>
          </div>
          <div class="ma-ar__track"><span class="ma-ar__fill ma-ar__fill--typ"></span></div>
        </div>
        <div class="ma-ar__row">
          <div class="ma-ar__top">
            <span class="ma-ar__lab">{{ section.settings.right_label | escape }}</span>
            <span class="ma-ar__val ma-ar__val--now">{{ now_value }}</span>
          </div>
          <div class="ma-ar__track"><span class="ma-ar__fill ma-ar__fill--now"></span></div>
        </div>
      </div>
      {%- if section.settings.refine_1 != blank or section.settings.refine_2 != blank -%}
        <div class="ma-ar__refine ma-ar__reveal">
          {%- if section.settings.refine_1 != blank -%}<p class="ma-ar__ref"><b>◆</b>{{ section.settings.refine_1 | escape }}</p>{%- endif -%}
          {%- if section.settings.refine_2 != blank -%}<p class="ma-ar__ref"><b>◆</b>{{ section.settings.refine_2 | escape }}</p>{%- endif -%}
        </div>
      {%- endif -%}
    {%- else -%}
      <!-- Style B: VS Comparison Cards -->
      <div class="ma-ar__cards ma-ar__reveal">
        <div class="ma-ar__card ma-ar__card--them">
          {%- if section.settings.card1_header != blank -%}<p class="ma-ar__card-h">{{ section.settings.card1_header | escape }}</p>{%- endif -%}
          <ul class="ma-ar__list">
            {%- assign left_items = value_blocks | where: 'settings.side', 'left' -%}
            {%- if left_items.size > 0 -%}
              {%- for block in left_items -%}
                <li class="ma-ar__list-item" {{ block.shopify_attributes }}>
                  <span class="ma-ar__list-icon">
                    <svg viewBox="0 0 24 24"><path d="M4 10l1-6h14l1 6M4 10v9h16v-9M4 10h16M9 19v-5h4v5"/></svg>
                  </span>
                  <span>{{ block.settings.text | escape }}</span>
                </li>
              {%- endfor -%}
            {%- else -%}
              {%- assign card1_items_array = section.settings.card1_items | newline_to_br | strip_newlines | split: '<br />' -%}
              {%- for item in card1_items_array -%}
                {%- assign trimmed = item | strip -%}
                {%- if trimmed != blank -%}
                  <li class="ma-ar__list-item">
                    <span class="ma-ar__list-icon"><svg viewBox="0 0 24 24"><path d="M4 10l1-6h14l1 6M4 10v9h16v-9M4 10h16M9 19v-5h4v5"/></svg></span>
                    <span>{{ trimmed }}</span>
                  </li>
                {%- endif -%}
              {%- endfor -%}
            {%- endif -%}
          </ul>
          <div class="ma-ar__card-foot">
            <span class="ma-ar__card-price">{{ section.settings.left_value | default: "₹20,000+" }}</span>
            <span class="ma-ar__card-sub">{{ section.settings.left_label | default: "Typical Luxury Perfume" }}</span>
          </div>
        </div>

        <span class="ma-ar__vs">{{ section.settings.vs_text | default: 'VS' | escape }}</span>

        <div class="ma-ar__card ma-ar__card--us">
          {%- if section.settings.card2_header != blank -%}<p class="ma-ar__card-h">{{ section.settings.card2_header | escape }}</p>{%- endif -%}
          <ul class="ma-ar__list">
            {%- assign right_items = value_blocks | where: 'settings.side', 'right' -%}
            {%- if right_items.size > 0 -%}
              {%- for block in right_items -%}
                <li class="ma-ar__list-item" {{ block.shopify_attributes }}>
                  <span class="ma-ar__list-icon ma-ar__ic--now">
                    <svg viewBox="0 0 24 24"><path d="M12 2s7 7.5 7 12.5A7 7 0 0 1 5 14.5C5 9.5 12 2 12 2z"/></svg>
                  </span>
                  <span>{{ block.settings.text | escape }}</span>
                </li>
              {%- endfor -%}
            {%- else -%}
              {%- assign card2_items_array = section.settings.card2_items | newline_to_br | strip_newlines | split: '<br />' -%}
              {%- for item in card2_items_array -%}
                {%- assign trimmed = item | strip -%}
                {%- if trimmed != blank -%}
                  <li class="ma-ar__list-item">
                    <span class="ma-ar__list-icon ma-ar__ic--now"><svg viewBox="0 0 24 24"><path d="M12 2s7 7.5 7 12.5A7 7 0 0 1 5 14.5C5 9.5 12 2 12 2z"/></svg></span>
                    <span>{{ trimmed }}</span>
                  </li>
                {%- endif -%}
              {%- endfor -%}
            {%- endif -%}
          </ul>
          <div class="ma-ar__card-foot">
            <span class="ma-ar__card-price">{{ now_value }}</span>
            <span class="ma-ar__card-sub">{{ section.settings.right_label | default: "Maison Amiir" }}</span>
          </div>
        </div>
      </div>
    {%- endif -%}

    <!-- PROOF GRID (4 COLUMNS HORIZONTAL ON DESKTOP) -->
    {%- if section.settings.enable_proof -%}
      <div class="ma-ar__proof ma-ar__reveal">
        {%- if proof_blocks.size > 0 -%}
          {%- for block in proof_blocks -%}
            <div class="ma-ar__proof-item" {{ block.shopify_attributes }}>
              <span class="ma-ar__proof-ic">
                <svg viewBox="0 0 24 24"><path d="M12 2s7 7.5 7 12.5A7 7 0 0 1 5 14.5C5 9.5 12 2 12 2z"/></svg>
              </span>
              <h3 class="ma-ar__proof-h">{{ block.settings.heading | escape }}</h3>
              <p class="ma-ar__proof-b">{{ block.settings.body | escape }}</p>
            </div>
          {%- endfor -%}
        {%- else -%}
          <div class="ma-ar__proof-item">
            <span class="ma-ar__proof-ic"><svg viewBox="0 0 24 24"><path d="M12 2s7 7.5 7 12.5A7 7 0 0 1 5 14.5C5 9.5 12 2 12 2z"/></svg></span>
            <h3 class="ma-ar__proof-h">30% Extrait Oil</h3>
            <p class="ma-ar__proof-b">The same concentration tier as the houses that inspire us.</p>
          </div>
          <div class="ma-ar__proof-item">
            <span class="ma-ar__proof-ic"><svg viewBox="0 0 24 24"><path d="M12 3l7 2.6v5.1c0 4.4-3 7.4-7 8.8-4-1.4-7-4.4-7-8.8V5.6L12 3z"/><path d="M9 12l2 2 4-4.2"/></svg></span>
            <h3 class="ma-ar__proof-h">IFRA-Compliant</h3>
            <p class="ma-ar__proof-b">Safe, kind to skin and crafted to international standards.</p>
          </div>
          <div class="ma-ar__proof-item">
            <span class="ma-ar__proof-ic"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3.5 2"/></svg></span>
            <h3 class="ma-ar__proof-h">Lasts All Day</h3>
            <p class="ma-ar__proof-b">High performance that stays with you from day to night.</p>
          </div>
          <div class="ma-ar__proof-item">
            <span class="ma-ar__proof-ic"><svg viewBox="0 0 24 24"><path d="M12 20s-7-4.4-9.3-8.8C1.2 8 2.7 4.8 6 4.2c2-.4 3.7.6 4.9 2.3 1.2-1.7 2.9-2.7 4.9-2.3 3.3.6 4.8 3.8 3.3 7C19 15.6 12 20 12 20z"/></svg></span>
            <h3 class="ma-ar__proof-h">Crafted with Intention</h3>
            <p class="ma-ar__proof-b">Small batch. Carefully blended. No shortcuts. Just great fragrance.</p>
          </div>
        {%- endif -%}
      </div>
    {%- endif -%}

    <!-- CONCLUSION & CROWN DIVIDER -->
    {%- if section.settings.conclusion_heading != blank or section.settings.conclusion_body != blank -%}
      <div class="ma-ar__conclusion ma-ar__reveal">
        <span class="ma-ar__crown">♛</span>
        {%- if section.settings.conclusion_heading != blank -%}<p class="ma-ar__conc-h">{{ section.settings.conclusion_heading | escape }}</p>{%- endif -%}
        {%- if section.settings.conclusion_body != blank -%}<p class="ma-ar__conc-b">{{ section.settings.conclusion_body | escape }}</p>{%- endif -%}
      </div>
    {%- endif -%}
  </div>

  <!-- RIGHT COLUMN (~45% DESKTOP): FULL HEIGHT PHOTOGRAPHY -->
  {%- if has_img -%}
    <div class="ma-ar__photo">
      {{ section.settings.bottle_image | image_url: width: 1600 | image_tag: loading: 'lazy', class: 'ma-ar__img', alt: section.settings.bottle_image.alt | default: shop.name }}
    </div>
  {%- endif -%}
</section>

<style>
  #ma-ar-{{ sid }}.ma-ar{
    --cream: {{ section.settings.bg_color | default: '#F4EEE1' }};
    --ink: {{ section.settings.ink_color | default: '#171310' }};
    --gold: {{ section.settings.gold_color | default: '#C6A15B' }};
    --burg: {{ section.settings.burg_color | default: '#6E1220' }};
    --burg2: {{ section.settings.burg2_color | default: '#8E1B28' }};
    --track: {{ section.settings.track_color | default: '#E6E0D5' }};
    --fill: {{ section.settings.fill_color | default: '#8E1B28' }};
    --ma-hdr: 0px;

    background: var(--cream);
    color: var(--ink);
    font-family: 'Cormorant Garamond', Georgia, serif;
    position: relative;
    overflow: hidden;
    box-sizing: border-box;
  }

  {% if section.settings.enable_texture or section.settings.show_texture %}
  #ma-ar-{{ sid }}.ma-ar::before{
    content: ''; position: absolute; inset: 0; z-index: 0; pointer-events: none;
    opacity: {{ section.settings.texture_opacity | default: 4 | divided_by: 100.0 }};
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='{{ section.settings.noise_amount | default: 4 | divided_by: 5.0 }}' numOctaves='3'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  }
  {% endif %}

  /* ---- Copy / Left Column Styling ---- */
  #ma-ar-{{ sid }} .ma-ar__left-col{
    box-sizing: border-box;
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }
  #ma-ar-{{ sid }} .ma-ar__k{
    font-family: {{ section.settings.eyebrow_font.family | default: 'Marcellus' }}, Georgia, serif;
    font-size: {{ section.settings.eyebrow_size_mob | default: 10 }}px;
    font-weight: {{ section.settings.eyebrow_weight | default: '400' }};
    letter-spacing: {{ section.settings.eyebrow_spacing | default: 3 }}px;
    text-transform: uppercase;
    color: var(--burg2);
    margin: 0 0 {{ section.settings.gap_head | default: 12 }}px;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  #ma-ar-{{ sid }} .ma-ar__k::before{
    content: "";
    width: {{ section.settings.divider_w_mob | default: 30 }}px;
    height: {{ section.settings.divider_thickness | default: 1 }}px;
    background: {{ section.settings.divider_color | default: 'var(--burg2)' }};
    flex: 0 0 auto;
    opacity: {{ section.settings.divider_opacity | default: 60 | divided_by: 100.0 }};
  }
  #ma-ar-{{ sid }} .ma-ar__h{
    font-family: {{ section.settings.heading_font.family | default: 'Cormorant Garamond' }}, Georgia, serif !important;
    font-weight: {{ section.settings.heading_weight | default: '600' }} !important;
    text-transform: none !important;
    letter-spacing: {{ section.settings.heading_spacing | default: 0 }}px !important;
    font-size: clamp(28px, min(4vw, 5.2vh), {{ section.settings.heading_size | default: 52 }}px);
    line-height: {{ section.settings.heading_lh | default: 108 | divided_by: 100.0 }};
    margin: 0 0 8px;
    color: var(--ink);
    max-width: {{ section.settings.heading_max_w | default: 24 }}ch;
  }
  #ma-ar-{{ sid }} .ma-ar__h em{
    font-style: {% if section.settings.accent_style == 'italic' %}italic{% else %}normal{% endif %};
    font-weight: {{ section.settings.accent_weight | default: '400' }};
    color: {{ section.settings.accent_color | default: 'var(--burg)' }};
  }
  #ma-ar-{{ sid }} .ma-ar__sub{
    font-family: {{ section.settings.sub_font.family | default: 'Cormorant Garamond' }}, Georgia, serif;
    font-style: italic;
    font-weight: {{ section.settings.sub_weight | default: '500' }};
    font-size: clamp(14px, 2vh, {{ section.settings.body_size | default: 18 }}px);
    line-height: 1.4;
    color: {{ section.settings.sub_color | default: 'rgba(23,19,16,0.78)' }};
    margin: 0 0 14px;
    max-width: 48ch;
  }

  /* ---- Comparison Cards ---- */
  #ma-ar-{{ sid }} .ma-ar__cmp{ margin: 0; width: 100%; }
  #ma-ar-{{ sid }} .ma-ar__row{ padding: 10px 0 0; border-top: 1px solid rgba(23,19,16,.14); }
  #ma-ar-{{ sid }} .ma-ar__top{ display: flex; justify-content: space-between; align-items: baseline; gap: 14px; }
  #ma-ar-{{ sid }} .ma-ar__lab{ font-family: 'Marcellus',serif; font-size: 10px; letter-spacing: .18em; text-transform: uppercase; color: rgba(23,19,16,.7); }
  #ma-ar-{{ sid }} .ma-ar__val{ font-family: 'Cormorant Garamond',Georgia,serif; white-space: nowrap; }
  #ma-ar-{{ sid }} .ma-ar__val--typ{ font-size: clamp(18px, 2.8vh, 24px); font-weight: 500; color: rgba(23,19,16,.44); text-decoration: line-through; text-decoration-thickness: 1.5px; }
  #ma-ar-{{ sid }} .ma-ar__val--now{ font-size: clamp(26px, min(8vw, 4.8vh), 42px); font-weight: 600; color: var(--burg2); }
  #ma-ar-{{ sid }} .ma-ar__val--now em{ font-style: italic; font-weight: 400; font-size: .52em; color: var(--burg); margin-right: 2px; }
  #ma-ar-{{ sid }} .ma-ar__track{ display: block; height: 4px; background: rgba(23,19,16,.08); border-radius: 2px; overflow: hidden; margin: 8px 0 10px; }
  #ma-ar-{{ sid }} .ma-ar__fill{ display: block; height: 100%; width: 0; border-radius: 2px; }
  #ma-ar-{{ sid }} .ma-ar__fill--typ{ background: rgba(23,19,16,.55); transition: width 1.4s cubic-bezier(.25,.8,.25,1) .1s; }
  #ma-ar-{{ sid }} .ma-ar__fill--now{ background: var(--burg2); transition: width 1.1s cubic-bezier(.25,.8,.25,1) .7s; }
  #ma-ar-{{ sid }}.is-in .ma-ar__fill--typ{ width: 100%; }
  #ma-ar-{{ sid }}.is-in .ma-ar__fill--now{ width: 12%; }
  #ma-ar-{{ sid }} .ma-ar__refine{ margin: 12px 0 0; display: flex; flex-direction: column; gap: 6px; }
  #ma-ar-{{ sid }} .ma-ar__ref{ font-size: 15px; font-weight: 500; color: rgba(23,19,16,.85); line-height: 1.4; margin: 0; }
  #ma-ar-{{ sid }} .ma-ar__ref b{ color: var(--gold); font-weight: 400; margin-right: 8px; }

  #ma-ar-{{ sid }} .ma-ar__cards{
    position: relative; display: flex; gap: clamp(12px, 1.5vw, {{ section.settings.card_gap | default: 24 }}px);
  }
  #ma-ar-{{ sid }} .ma-ar__card{
    flex: 1 1 0; min-width: 0; padding: clamp(14px, 2vh, {{ section.settings.card_padding | default: 20 }}px);
    border-radius: {{ section.settings.card_radius | default: 8 }}px;
    background: {{ section.settings.card_bg_col | default: '#F0EAE1' }};
    border: 1px solid {{ section.settings.card_border_color | default: '#E2DCD3' }};
    {% if section.settings.card_shadow_strength > 0 %}box-shadow: 0 {{ section.settings.card_shadow_strength }}px {{ section.settings.card_shadow_strength | times: 2 }}px rgba(23,19,16,{{ section.settings.card_shadow_strength | times: 1.0 | divided_by: 200 }});{% endif %}
  }
  #ma-ar-{{ sid }} .ma-ar__card--us {
    background: linear-gradient(145deg, rgba(244,238,225,0.95), rgba(244,238,225,0.65));
    border-color: rgba(198,161,91,0.45);
    box-shadow: 0 12px 30px -10px rgba(0,0,0,0.08);
  }
  #ma-ar-{{ sid }} .ma-ar__card-h{
    font-family: 'Marcellus',serif; font-size: 10px; letter-spacing: .14em; text-transform: uppercase;
    color: rgba(23,19,16,.65); margin: 0 0 10px; padding-bottom: 8px; border-bottom: 1px solid rgba(23,19,16,.12);
  }
  #ma-ar-{{ sid }} .ma-ar__card--us .ma-ar__card-h { color: var(--burg2); border-color: rgba(198,161,91,0.25); }
  #ma-ar-{{ sid }} .ma-ar__list{ list-style: none; margin: 0 0 10px; padding: 0; display: flex; flex-direction: column; gap: 6px; }
  #ma-ar-{{ sid }} .ma-ar__list-item{ display: flex; align-items: center; gap: 8px; font-size: clamp(12px, 1.4vh, 13.5px); color: rgba(23,19,16,.85); }
  #ma-ar-{{ sid }} .ma-ar__list-icon{ flex: 0 0 auto; width: 15px; height: 15px; color: var(--gold); display: flex; align-items: center; }
  #ma-ar-{{ sid }} .ma-ar__card--them .ma-ar__list-icon{ color: rgba(23,19,16,0.4); }
  #ma-ar-{{ sid }} .ma-ar__list-icon svg{ width: 100%; height: 100%; fill: none; stroke: currentColor; stroke-width: 1.6; stroke-linecap: round; stroke-linejoin: round; }
  #ma-ar-{{ sid }} .ma-ar__card-foot{ padding-top: 8px; border-top: 1px solid rgba(23,19,16,.12); display: flex; flex-direction: column; gap: 2px; }
  #ma-ar-{{ sid }} .ma-ar__card--us .ma-ar__card-foot { border-color: rgba(198,161,91,0.25); }
  #ma-ar-{{ sid }} .ma-ar__card-price{ font-family: 'Cormorant Garamond',Georgia,serif; font-size: clamp(22px, 2.8vh, 26px); font-weight: 600; line-height: 1; color: var(--ink); }
  #ma-ar-{{ sid }} .ma-ar__card--us .ma-ar__card-price { color: var(--burg2); font-size: clamp(26px, 3.2vh, 32px); }
  #ma-ar-{{ sid }} .ma-ar__card-sub{ font-size: 11px; color: rgba(23,19,16,.65); font-style: italic; }
  #ma-ar-{{ sid }} .ma-ar__vs{
    position: absolute; left: 50%; top: 50%; transform: translate(-50%,-50%);
    font-family: 'Marcellus',serif; font-size: 12px; letter-spacing: .08em; color: var(--gold);
    background: var(--cream); padding: 4px 8px; border-radius: 50%; z-index: 2;
  }

  /* ---- Proof Grid ---- */
  #ma-ar-{{ sid }} .ma-ar__proof{
    display: grid; gap: clamp(8px, 1.2vw, {{ section.settings.proof_gap | default: 16 }}px);
    {% if section.settings.proof_divider %}border-top: 1px solid rgba(23,19,16,.12); padding-top: 12px;{% endif %}
  }
  #ma-ar-{{ sid }} .ma-ar__proof-item{ display: flex; flex-direction: column; gap: 3px; padding-right: 6px; }
  #ma-ar-{{ sid }} .ma-ar__proof-ic{ display: block; width: {{ section.settings.proof_icon_size | default: 22 }}px; height: {{ section.settings.proof_icon_size | default: 22 }}px; color: var(--gold); margin-bottom: 6px; }
  #ma-ar-{{ sid }} .ma-ar__proof-ic svg{ width: 100%; height: 100%; fill: none; stroke: currentColor; stroke-width: 1.3; stroke-linecap: round; stroke-linejoin: round; }
  #ma-ar-{{ sid }} .ma-ar__proof-h{ font-family: 'Cormorant Garamond',Georgia,serif; font-weight: 600; font-size: clamp(14px, 1.8vh, 16px); margin: 0 0 2px; color: var(--ink); line-height: 1.2; }
  #ma-ar-{{ sid }} .ma-ar__proof-b{ font-size: clamp(11.5px, 1.3vh, 12.5px); line-height: 1.35; color: rgba(23,19,16,.7); margin: 0; }

  /* ---- Conclusion ---- */
  #ma-ar-{{ sid }} .ma-ar__conclusion{ text-align: center; padding-top: 10px; border-top: 1px solid rgba(23,19,16,.12); }
  #ma-ar-{{ sid }} .ma-ar__crown{ display: block; font-size: 16px; color: var(--gold); margin-bottom: 2px; }
  #ma-ar-{{ sid }} .ma-ar__conc-h{ font-family: 'Cormorant Garamond',Georgia,serif; font-size: clamp(18px, 2.2vh, {{ section.settings.conclusion_size | default: 22 }}px); font-weight: 600; margin: 0 0 2px; color: var(--ink); line-height: 1.2; }
  #ma-ar-{{ sid }} .ma-ar__conc-b{ font-style: italic; font-size: clamp(13px, 1.6vh, 15px); color: var(--burg); margin: 0; }

  /* ---- Reveal Animation ---- */
  {% if section.settings.enable_animation %}
    #ma-ar-{{ section.id }} .ma-ar__reveal{
      opacity: 0; transform: translateY(12px);
      transition: opacity {{ section.settings.animation_duration | default: 600 }}ms ease, transform {{ section.settings.animation_duration | default: 600 }}ms cubic-bezier(.22,1,.36,1);
    }
    #ma-ar-{{ section.id }}.is-in .ma-ar__reveal{ opacity: 1; transform: none; }
    #ma-ar-{{ section.id }}.is-in .ma-ar__cards{ transition-delay: 0ms; }
    #ma-ar-{{ section.id }}.is-in .ma-ar__proof{ transition-delay: {{ section.settings.animation_stagger | default: 80 }}ms; }
    #ma-ar-{{ section.id }}.is-in .ma-ar__conclusion{ transition-delay: {{ section.settings.animation_stagger | default: 80 | times: 2 }}ms; }
  {% endif %}

  /* ---- Photo & Melt Engine ---- */
  #ma-ar-{{ section.id }} .ma-ar__photo{ overflow: hidden; position: relative; }
  #ma-ar-{{ section.id }} .ma-ar__img{
    width: 100%; height: 100%; object-fit: cover; display: block;
    object-position: {{ section.settings.image_focal_x | default: 50 }}% {{ section.settings.image_focal_y | default: 50 }}%;
    transform: scale({{ section.settings.image_scale | default: 100 | times: 1.0 | divided_by: 100 }}) translate({{ section.settings.image_offset_x | default: 0 }}px, {{ section.settings.image_offset_y | default: 0 }}px);
    filter: brightness({{ section.settings.img_brightness | default: 100 }}%) contrast({{ section.settings.img_contrast | default: 100 }}%) saturate({{ section.settings.img_saturation | default: 100 }}%) sepia({{ section.settings.img_warmth | default: 0 | at_least: 0 | times: 1.0 | divided_by: 100 }});
  }

  /* ---- MOBILE (< 900px): Stacked Layout ---- */
  @media (max-width: 899px){
    #ma-ar-{{ section.id }}.ma-ar{ display: flex; flex-direction: column; height: auto; max-height: none; overflow: visible; }
    #ma-ar-{{ section.id }} .ma-ar__left-col{
      flex: 0 0 auto; justify-content: flex-start;
      padding: {{ section.settings.mobile_padding_top | default: 24 }}px {{ section.settings.mobile_padding_x | default: 24 }}px {{ section.settings.mobile_padding_bottom | default: 32 }}px;
    }
    #ma-ar-{{ section.id }} .ma-ar__photo{
      order: {% if section.settings.mobile_image_first %}-1{% else %}1{% endif %};
      height: {{ section.settings.mobile_image_height | default: 280 }}px; width: 100%;
    }
    #ma-ar-{{ section.id }} .ma-ar__cards{ flex-direction: column; gap: 16px; }
    #ma-ar-{{ section.id }} .ma-ar__vs{ position: relative; left: auto; top: auto; transform: none; margin: -6px auto; display: block; width: fit-content; }
    #ma-ar-{{ section.id }} .ma-ar__proof{ grid-template-columns: 1fr 1fr; gap: 16px; }
    {%- if melt > 0 -%}
    #ma-ar-{{ section.id }} .ma-ar__photo::before{
      content: ""; position: absolute; z-index: 2; left: 0; right: 0; bottom: 0; height: 34%; pointer-events: none;
      backdrop-filter: blur({{ melt_blur }}px); -webkit-backdrop-filter: blur({{ melt_blur }}px);
      -webkit-mask-image: linear-gradient(0deg,#000 0,transparent 100%);
              mask-image: linear-gradient(0deg,#000 0,transparent 100%);
    }
    #ma-ar-{{ section.id }} .ma-ar__photo::after{
      content: ""; position: absolute; z-index: 3; inset: 0; pointer-events: none; opacity: {{ melt_veil }};
      background: linear-gradient(0deg,var(--cream) 0%,rgba(244,238,225,.55) 14%,rgba(244,238,225,0) 46%);
    }
    {%- endif -%}
  }

  /* ---- DESKTOP (>= 900px): SINGLE VIEWPORT 100VH TWO-COLUMN MAGAZINE SPREAD ---- */
  @media (min-width: 900px){
    #ma-ar-{{ section.id }}.ma-ar{
      display: grid !important;
      grid-template-columns: {{ split_left }}% {{ split_right }}% !important;
      height: calc(100vh - var(--ma-hdr)) !important;
      max-height: calc(100vh - var(--ma-hdr)) !important;
      overflow: hidden !important;
    }
    #ma-ar-{{ section.id }} .ma-ar__left-col{
      height: 100% !important; max-height: 100% !important;
      padding: {{ section.settings.padding_top | default: 24 }}px {{ section.settings.padding_x | default: 48 }}px {{ section.settings.padding_bottom | default: 24 }}px {{ section.settings.padding_x | default: 48 | times: 1.1 }}px !important;
      overflow: hidden !important; box-sizing: border-box !important;
    }
    #ma-ar-{{ section.id }} .ma-ar__proof{
      grid-template-columns: repeat(4, 1fr) !important;
    }
    #ma-ar-{{ section.id }} .ma-ar__proof-item:not(:last-child){
      border-right: 1px solid rgba(23,19,16,.08);
    }
    #ma-ar-{{ section.id }} .ma-ar__photo{
      height: 100% !important; width: 100% !important; position: relative !important; overflow: hidden !important;
    }
    {%- if melt > 0 -%}
    #ma-ar-{{ section.id }} .ma-ar__photo::before{
      content: ""; position: absolute; z-index: 2; top: 0; bottom: 0; left: 0; width: {{ section.settings.melt_width | default: 40 }}%; pointer-events: none;
      backdrop-filter: blur({{ melt_blur }}px); -webkit-backdrop-filter: blur({{ melt_blur }}px);
      -webkit-mask-image: linear-gradient(90deg,#000 0,transparent 100%);
              mask-image: linear-gradient(90deg,#000 0,transparent 100%);
    }
    #ma-ar-{{ section.id }} .ma-ar__photo::after{
      content: ""; position: absolute; z-index: 3; inset: 0; pointer-events: none; opacity: {{ melt_veil }};
      background: linear-gradient(90deg,var(--cream) 0%,rgba(244,238,225,.6) 12%,rgba(244,238,225,0) 48%);
    }
    {%- endif -%}
  }

  @media (prefers-reduced-motion: reduce){
    #ma-ar-{{ section.id }} .ma-ar__fill{ transition: none !important; }
    #ma-ar-{{ section.id }} .ma-ar__fill--typ{ width: 100% !important; }
    #ma-ar-{{ section.id }} .ma-ar__fill--now{ width: 12% !important; }
    #ma-ar-{{ section.id }} .ma-ar__reveal{ opacity: 1 !important; transform: none !important; transition: none !important; }
  }
</style>

<script>
  (function(){
    var s = document.getElementById('ma-ar-{{ section.id }}');
    if(!s) return;

    function fitHeader(){
      var off = 0;
      ['sticky-header','.shopify-section-group-header-group','.section-header','header'].some(function(sel){
        var el = document.querySelector(sel);
        if(!el) return false;
        var cs = getComputedStyle(el);
        if(cs.position === 'sticky' || cs.position === 'fixed'){ off = el.getBoundingClientRect().height; return true; }
        return false;
      });
      s.style.setProperty('--ma-hdr', off + 'px');
    }
    fitHeader();
    window.addEventListener('resize', fitHeader);

    {% if request.design_mode %}
      s.classList.add('is-in'); return;
    {% endif %}
    if(!('IntersectionObserver' in window)){ s.classList.add('is-in'); return; }
    var io = new IntersectionObserver(function(entries){
      entries.forEach(function(e){
        if(e.isIntersecting){ s.classList.add('is-in'); }
        else { s.classList.remove('is-in'); }
      });
    }, { threshold: 0.3 });
    io.observe(s);
  })();
</script>

{% schema %}
{
  "name": "MA — Honest Arithmetic",
  "tag": "section",
  "class": "ma-arith-section",
  "settings": [
    { "type": "paragraph", "content": "Single-viewport editorial split. Photo melts into the cream — tune it below." },
    { "type": "image_picker", "id": "bottle_image", "label": "Bottle photo" },
    { "type": "range", "id": "melt", "label": "Photo melt intensity", "min": 0, "max": 100, "step": 10, "unit": "%", "default": 100 },

    {
      "type": "header",
      "content": "Content"
    },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Luxury, Without the Markup Price" },
    { "type": "textarea", "id": "heading", "label": "Headline", "default": "Luxury belongs inside the bottle. Not on the receipt." },
    { "type": "text", "id": "subheading", "label": "Sub-line", "default": "Great fragrance is mostly oil and intention — not the marble store or the magazine." },

    {
      "type": "header",
      "content": "Comparison style"
    },
    { "type": "select", "id": "comparison_style", "label": "Comparison style", "default": "cards", "options": [
      { "value": "cards", "label": "Icon-list cards (new)" },
      { "value": "bars", "label": "Progress bars (legacy)" }
    ]},
    { "type": "text", "id": "vs_text", "label": "Divider text", "default": "VS" },

    {
      "type": "header",
      "content": "Comparison — Card 1"
    },
    { "type": "text", "id": "card1_header", "label": "Card 1 title", "default": "Where Your Money Usually Goes" },
    { "type": "textarea", "id": "card1_items", "label": "Card 1 bullet items (one per line, up to 5)", "default": "Luxury Stores & Rent\nCelebrity Endorsements\nMassive Advertising\nPremium Packaging Overkill\nRetail Markup" },
    { "type": "text", "id": "left_label", "label": "Card 1 caption", "default": "Typical Luxury Perfume" },
    { "type": "text", "id": "left_value", "label": "Card 1 price", "default": "₹20,000+" },

    {
      "type": "header",
      "content": "Comparison — Card 2"
    },
    { "type": "text", "id": "card2_header", "label": "Card 2 title", "default": "What You're Really Paying For" },
    { "type": "textarea", "id": "card2_items", "label": "Card 2 bullet items (one per line, up to 5)", "default": "Fine Fragrance Oil (30% Extrait)\nPremium Ingredients\nArtisan Blending\nQuality Bottle & Packaging\nFair Pricing" },
    { "type": "text", "id": "right_label", "label": "Card 2 caption", "default": "Maison Amiir" },
    { "type": "text", "id": "right_value", "label": "Card 2 price", "default": "from ₹2,200" },

    {
      "type": "header",
      "content": "Legacy refinements (bars style only)"
    },
    { "type": "text", "id": "refine_1", "label": "Refinement 1", "default": "Lasts all day · 30% extrait" },
    { "type": "text", "id": "refine_2", "label": "Refinement 2", "default": "Kind to skin · IFRA-compliant oils" },

    {
      "type": "header",
      "content": "Proof grid — 4 items, always 4 across on desktop"
    },
    { "type": "select", "id": "proof1_icon", "label": "Proof 1 icon", "default": "droplet", "options": [
      { "value": "droplet", "label": "Droplet" }, { "value": "shield", "label": "Shield" }, { "value": "clock", "label": "Clock" }, { "value": "heart", "label": "Heart" }, { "value": "leaf", "label": "Leaf" }, { "value": "none", "label": "None" }
    ]},
    { "type": "text", "id": "proof1_heading", "label": "Proof 1 heading", "default": "30% Extrait Oil" },
    { "type": "text", "id": "proof1_body", "label": "Proof 1 body", "default": "The same concentration tier as the houses that inspire us." },

    { "type": "select", "id": "proof2_icon", "label": "Proof 2 icon", "default": "shield", "options": [
      { "value": "droplet", "label": "Droplet" }, { "value": "shield", "label": "Shield" }, { "value": "clock", "label": "Clock" }, { "value": "heart", "label": "Heart" }, { "value": "leaf", "label": "Leaf" }, { "value": "none", "label": "None" }
    ]},
    { "type": "text", "id": "proof2_heading", "label": "Proof 2 heading", "default": "IFRA-Compliant" },
    { "type": "text", "id": "proof2_body", "label": "Proof 2 body", "default": "Safe, kind to skin and crafted to international standards." },

    { "type": "select", "id": "proof3_icon", "label": "Proof 3 icon", "default": "clock", "options": [
      { "value": "droplet", "label": "Droplet" }, { "value": "shield", "label": "Shield" }, { "value": "clock", "label": "Clock" }, { "value": "heart", "label": "Heart" }, { "value": "leaf", "label": "Leaf" }, { "value": "none", "label": "None" }
    ]},
    { "type": "text", "id": "proof3_heading", "label": "Proof 3 heading", "default": "Lasts All Day" },
    { "type": "text", "id": "proof3_body", "label": "Proof 3 body", "default": "High performance that stays with you from day to night." },

    { "type": "select", "id": "proof4_icon", "label": "Proof 4 icon", "default": "heart", "options": [
      { "value": "droplet", "label": "Droplet" }, { "value": "shield", "label": "Shield" }, { "value": "clock", "label": "Clock" }, { "value": "heart", "label": "Heart" }, { "value": "leaf", "label": "Leaf" }, { "value": "none", "label": "None" }
    ]},
    { "type": "text", "id": "proof4_heading", "label": "Proof 4 heading", "default": "Crafted with Intention" },
    { "type": "text", "id": "proof4_body", "label": "Proof 4 body", "default": "Small batch. Carefully blended. No shortcuts. Just great fragrance." },

    {
      "type": "header",
      "content": "Conclusion"
    },
    { "type": "text", "id": "conclusion_heading", "label": "Conclusion heading", "default": "Same luxury. Better value." },
    { "type": "text", "id": "conclusion_body", "label": "Conclusion body", "default": "Because you deserve the fragrance, not the markup." },

    {
      "type": "header",
      "content": "Image"
    },
    { "type": "range", "id": "image_scale", "label": "Image scale", "min": 100, "max": 140, "step": 5, "default": 100, "unit": "%" },
    { "type": "range", "id": "image_offset_x", "label": "Image offset X", "min": -60, "max": 60, "step": 5, "default": 0, "unit": "px" },
    { "type": "range", "id": "image_offset_y", "label": "Image offset Y", "min": -60, "max": 60, "step": 5, "default": 0, "unit": "px" },
    { "type": "range", "id": "image_focal_x", "label": "Image focal point X", "min": 0, "max": 100, "step": 5, "default": 50, "unit": "%" },
    { "type": "range", "id": "image_focal_y", "label": "Image focal point Y", "min": 0, "max": 100, "step": 5, "default": 50, "unit": "%" },

    {
      "type": "header",
      "content": "Image art direction"
    },
    { "type": "range", "id": "img_brightness", "label": "Brightness", "min": 70, "max": 130, "step": 5, "default": 100, "unit": "%" },
    { "type": "range", "id": "img_contrast", "label": "Contrast", "min": 70, "max": 130, "step": 5, "default": 100, "unit": "%" },
    { "type": "range", "id": "img_saturation", "label": "Saturation", "min": 0, "max": 160, "step": 10, "default": 100, "unit": "%" },
    { "type": "range", "id": "img_warmth", "label": "Warmth", "min": 0, "max": 30, "step": 5, "default": 0, "unit": "%" },
    { "type": "range", "id": "melt_width", "label": "Melt width (desktop)", "min": 20, "max": 60, "step": 5, "default": 40, "unit": "%" },

    {
      "type": "header",
      "content": "Comparison card styling"
    },
    { "type": "range", "id": "card_radius", "label": "Card corner radius", "min": 0, "max": 24, "step": 2, "default": 8, "unit": "px" },
    { "type": "color", "id": "card_border_color", "label": "Card border color", "default": "#e8e0d0" },
    { "type": "range", "id": "card_shadow_strength", "label": "Card shadow strength", "min": 0, "max": 30, "step": 5, "default": 0 },
    { "type": "range", "id": "card_padding", "label": "Card padding", "min": 14, "max": 40, "step": 2, "default": 20, "unit": "px" },
    { "type": "range", "id": "card_gap", "label": "Gap between cards", "min": 12, "max": 60, "step": 4, "default": 24, "unit": "px" },

    {
      "type": "header",
      "content": "Proof grid styling"
    },
    { "type": "range", "id": "proof_gap", "label": "Proof grid gap", "min": 12, "max": 60, "step": 2, "default": 16, "unit": "px" },
    { "type": "range", "id": "proof_icon_size", "label": "Proof icon size", "min": 18, "max": 40, "step": 2, "default": 22, "unit": "px" },
    { "type": "checkbox", "id": "proof_divider", "label": "Show divider above proof grid", "default": true },

    {
      "type": "header",
      "content": "Layout — Desktop"
    },
    { "type": "select", "id": "split_ratio", "label": "Content / image split", "default": "55/45", "options": [
      { "value": "55/45", "label": "55% / 45%" },
      { "value": "54/46", "label": "54% / 46%" },
      { "value": "53/47", "label": "53% / 47%" },
      { "value": "52/48", "label": "52% / 48%" }
    ]},
    { "type": "range", "id": "headline_size", "label": "Headline size (desktop)", "min": 28, "max": 64, "step": 2, "default": 52, "unit": "px" },
    { "type": "range", "id": "body_size", "label": "Sub-line size (desktop)", "min": 14, "max": 22, "step": 1, "default": 18, "unit": "px" },
    { "type": "range", "id": "conclusion_size", "label": "Conclusion heading size", "min": 18, "max": 32, "step": 2, "default": 22, "unit": "px" },

    {
      "type": "header", "content": "Spacing — Desktop"
    },
    { "type": "range", "id": "padding_top", "label": "Padding top", "min": 0, "max": 60, "step": 4, "default": 24, "unit": "px" },
    { "type": "range", "id": "padding_bottom", "label": "Padding bottom", "min": 0, "max": 60, "step": 4, "default": 24, "unit": "px" },
    { "type": "range", "id": "padding_x", "label": "Padding left/right", "min": 24, "max": 100, "step": 4, "default": 48, "unit": "px" },

    {
      "type": "header", "content": "Layout — Mobile (independent)"
    },
    { "type": "checkbox", "id": "mobile_image_first", "label": "Image first on mobile", "default": true },
    { "type": "range", "id": "mobile_image_height", "label": "Image height (mobile)", "min": 200, "max": 500, "step": 10, "default": 280, "unit": "px" },
    { "type": "range", "id": "mobile_padding_top", "label": "Padding top (mobile)", "min": 12, "max": 48, "step": 4, "default": 24, "unit": "px" },
    { "type": "range", "id": "mobile_padding_bottom", "label": "Padding bottom (mobile)", "min": 16, "max": 60, "step": 4, "default": 32, "unit": "px" },
    { "type": "range", "id": "mobile_padding_x", "label": "Padding left/right (mobile)", "min": 16, "max": 40, "step": 2, "default": 24, "unit": "px" },

    {
      "type": "header", "content": "Background"
    },
    { "type": "color", "id": "bg_color", "label": "Background color", "default": "#F4EEE1" },
    { "type": "checkbox", "id": "enable_texture", "label": "Enable subtle paper texture", "default": false },
    { "type": "range", "id": "texture_opacity", "label": "Texture opacity", "min": 1, "max": 15, "step": 1, "default": 4 },

    {
      "type": "header", "content": "Animation"
    },
    { "type": "checkbox", "id": "enable_animation", "label": "Enable reveal animation", "default": true },
    { "type": "range", "id": "animation_stagger", "label": "Stagger delay", "min": 0, "max": 200, "step": 10, "default": 80, "unit": "ms" },
    { "type": "range", "id": "animation_duration", "label": "Animation duration", "min": 200, "max": 1200, "step": 50, "default": 600, "unit": "ms" }
  ],
  "presets": [{ "name": "MA — Honest Arithmetic" }]
}
{% endschema %}