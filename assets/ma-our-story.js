/* MAISON AMIIR — scroll-reveal animations removed by request (2026-07-03):
   the Our Story page renders static and immediately visible (the hidden
   state was gated behind a class this script no longer adds). File kept so
   the section's script tag doesn't 404. */
